spring:
  application:
    name: message-service
  data:
    mongodb:
      uri: ${URI}
#      database: ${DB}
#      username: ${USERNAME}
#      password: ${PASSWORD}
#      host: ${HOST}
#      port: ${PORT}
#      authentication-database: ${AUTH_DB}

server:
  port: 8081