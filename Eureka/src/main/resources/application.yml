spring:
  application:
    name: Eureka
#  config:
#    import: "optional:configserver:${CONFIG_URI}"

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761
