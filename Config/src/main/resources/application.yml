spring:
  application:
    name: Config
  cloud:
    config:
      server:
        git:
          uri: ${GITHUB_CONFIG_URI}
          username: WilliamBC24
          password: ${GITHUB_PASS}

server:
  port: 8100

logging:
  level:
    org.springframework.cloud: DEBUG
